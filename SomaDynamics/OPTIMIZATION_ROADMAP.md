# Soma Dynamics 参数与架构长期优化路线

## 目标

让普通用户用 3 个有明确感知含义的参数得到稳定肉感，高级用户仍能无损访问逐骨和
求解器参数；所有手感变化都能用固定动作、日志和预设回归验证。

## 参数分层

1. 用户层：强度、柔软度、动作响应。
2. 部位层：大腿、手臂、小肚子各自的映射曲线和安全范围。
3. 求解器层：Weight、Damping、Elasticity、Stiffness、Inert、JitterFreq、
   MotionSmooth、Gravity。
4. 骨骼层：Amp、Rot、RC、XYZ，仅在高级模式出现。

简单参数不是额外叠乘系数，而是求解器参数的唯一成组编辑入口；高级参数修改后仍可
被完整保存。这样不会出现两个不同来源同时控制同一个效果的隐藏状态。

## 当前基线（1.0.2.4）

`自然` 基线最初提炼自本机最近保存的 `NEW.xml` 常用参数；该 XML 不随插件发布：

| 部位 | 模式 | Weight | Damping | Elasticity | Stiffness | Inert | JitterFreq |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 大腿 | Chain | 0.90 | 0.04 | 0.05 | 0.85 | 0.80 | 0.20 |
| 手臂 | Chain | 0.70 | 0.05 | 0.25 | 0.90 | 0.80 | 0.15 |
| 小肚子 | Chain | 0.70 | 0.30 | 0.25 | 0.90 | 0.40 | 1.00 |

这组数值是 `自然` 预设与新角色默认；柔软度扫描和确定性舞蹈驱动已证明三个部位的
链式与弹簧动态响应均通过五点单调门禁，并完成 30/60/高帧率标定；仍会继续用
更多动作和极端体型做扩展标定。

## 阶段计划

### P1：参数入口收敛（已完成第一版）

- 简单 UI 默认只显示三个参数；
- 高级 UI 保持旧卡/旧预设可编辑；
- 清除求解器未使用的内存字段与序列化写入；
- 所有外部输入统一限幅。

### P2：动作回归场景（已完成第一版）

- 参数模型回归已落地：默认基线、简单参数往返、单调性、三档预设、输入限幅和
  旧键兼容、旧卡迁移、时间步和预设求解器语义共 574 项断言，并接入正式构建；
- 确定性 0.5 Hz 转动/位移驱动覆盖大腿、手臂、小肚子；
- 每五秒记录 RMS、静态偏移、动态 RMS、峰值、NaN 重置和重锚次数；
- 插件内置动作驱动与自动载入场景已移除；运行体验由用户手动测试，仓库只保留静态参数
  测试和离线日志分析工具。

### P3：参数曲线标定（单角色双求解器完成）

- Chain 与 Spring 已对柔软度 0/0.25/0.5/0.75/1 做实机采样，三个部位均通过
  动态单调性、峰值、零重置与零重锚门禁；
- 两种求解器已通过 30/60/高帧率响应差异门禁；Chain 已覆盖两张女性卡和一张
  Bulky 男性卡，Spring 已覆盖女性/男性，并通过跨卡动态差异与安全矩阵；
- 下一步继续加入纤细、矮小、极端 ABMX 等体型与更多动作，而不是只扩充同类卡数量；
- 保证强度单调增加峰值、柔软度单调增加滞后且不导致拉伸；
- 为弹簧和链式分别拟合曲线，不强求两种求解器数值相同，只保证感知相近。

### P4：预设重做（已完成第一版）

- 已删除“Soft=Weight 0”这类名不副实的旧内置 XML；
- 简单 UI 只提供低、中、高三档全身目标；档位不改写各部位求解模式；
- 三档由版本化映射生成；高级 XML 只用于用户自定义和旧文件兼容。

### P5：核心拆分与性能（第一阶段完成）

- 已拆出 Bone/Chain State、StateReset、SafetyGuard、Metrics、FleshSpringSolver、
  FleshChainSolver 和公共标量映射；主编排文件从 1483 行降至约 1171 行；
- 单/多粒子链共享速度积分与 leash，Spring/Chain 重锚、禁用和异常恢复共享统一复位；
- 已建立每角色/每部位 CPU 平均/峰值耗时与 CSV 基线；旧 Mono 环境下求解器区间
  未观察到堆增长，但仍需在支持精确分配计数的运行时复核，不把它表述为绝对零分配。

## 验收原则

- 新用户加载角色后无需打开高级模式即可得到可用效果；
- 旧卡、旧 XML、GUID 和角色卡保存流程不破坏；
- 简单参数的方向必须稳定、可解释、单调；
- 任意 UI/预设输入都不能把超范围或非有限值送入物理更新；
- 物理关闭或清除后，位置与旋转回到角色卡姿态。
