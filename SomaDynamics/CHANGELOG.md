# 更新日志（CHANGELOG）

## 1.0.3.0-KKS（2026-08-13）

- 与 KK 版 1.0.3.0 同步（同一份源码）：预设一键保存/应用与默认预设自动套用；五部位
  默认启用开关（可全局覆盖）；Timeline 弹簧三档（关闭/手动/自动）与自定义快捷键。
- 已在 KKS 本地沙盒（主游戏 + CharaStudio）验证加载与初始化正常。

## 1.0.2.7-KKS（2026-08-13）

- 独立 KKS 仓库（DevoLutioner/SomaDynamics-KKS）首次发布：与 KK 版共用同一份源码，
  依赖 KKSAPI 与 KKS_ExtensibleSaveFormat（两者命名空间与 KKAPI 一致），目标框架
  net471，游戏根目录默认 `H:\KKS\KKS`，可用 `-GameRoot` 或 `KKS_BUILD_GAME_ROOT`
  覆盖，产物为 `SomaDynamics_KKS_<版本>`。
- 已在 KKS 本地沙盒（主游戏 + CharaStudio）验证：插件加载、初始化、Harmony 补丁与
  Studio 场景钩子安装全部正常。

## 1.0.2.7（2026-08-11）

- Soma 面板新增 `播放 Timeline 时 Chain 临时切换为弹簧` 兼容开关，默认关闭；遇到某些
  作者的 Timeline 一播放就让四肢扭曲时，可按场景手动开启。
- 开关开启后只在 Timeline 实际播放期间让大腿、手臂、腹部的 Chain 临时使用 Spring；
  暂停或停止时自动恢复原 Chain，且不会改写角色卡或 XML 保存的求解模式。
- 切换进出兼容状态时先撤销 Soma 自己上一帧的 Chain 形变并重新捕获基准，避免模式交接
  留下旧旋转、速度或永久形变；面板会显示待命/正在临时使用 Spring 的实时状态。

## 1.0.2.4（2026-08-11）

- 修复 Timeline 直接大幅移动或旋转整个角色时，小腿和手臂 Chain 被瞬移速度拉出严重
  形变：新增锚点世界空间跳变检测，超过 8 cm 或 35° 时整链原位重锚一帧。
- 角色整体瞬移不再作为 Verlet 惯性输入；下一帧立即恢复正常 Chain 物理，连续小幅运动
  仍保留原有弹性和滞后。

## 1.0.2.3（2026-08-11）

- 修复 1.0.2.2 在 Timeline 每帧直接写形体骨时 Chain 效果被完全让权的问题：普通连续
  关键帧现在作为实时基准继续进入 Chain，Soma 仍在其上叠加位移和 RC 物理。
- 只有单帧局部位移超过 8 cm 或局部旋转超过 35° 的姿势切换才清零整链一帧；连续动作
  不再被误判为场景跳转，同时保留 1.0.2.2 的无反馈干净基准求解。

## 1.0.2.2（2026-08-11）

- 修复 Chain 在 Timeline 暂停后仍缓慢变形：每帧求解前先剥离 Soma 上一帧写入，在未形变
  骨架上计算整条链，再只叠加一次当前帧结果，切断父骨 RC/位移进入子骨静止基准的反馈。
- 检测运行时 Spring → Chain 模式切换，等待当前姿势稳定并重建 Chain；不再复用上次 Chain
  的粒子和旋转状态。Chain → Spring 时先撤销 Chain 自身形变，再初始化 Spring 基准。

## 1.0.2.1（2026-08-11）

- 直接订阅 KKAPI 的 Studio 场景 `Load/Import/Clear` 完成事件，Timeline 场景载入不再依赖
  `OCIChar.LoadAnime` 是否被调用；新增 `SOMA_SCENE_REBASE` 日志确认场景级重锚。
- 修复 1.0.2.0 在 Timeline 直接写形体骨时可能出现的手臂自转：检测到任一外部位置或旋转
  写入后，整条 Chain 原位清除速度和 RC 目标，并在该帧让出骨骼所有权。
- Timeline 持续直接控制形体骨时由 Timeline 优先；停止直接写入后 Chain 自动恢复，避免两个
  系统逐帧争夺同一 Transform。

## 1.0.2.0（2026-08-11）

- 修复 Timeline 场景载入后 Chain 模式大腿扭曲：首粒子现在也会识别外部旋转写入，
  不再用场景载入前的旧 RC 基准覆盖 Timeline 姿势。
- Studio/Timeline 切换姿势时不再强制恢复角色卡 Pristine Pose；自动路径只撤销 Soma
  自己的上一帧形变，并在当前姿势连续稳定两帧后重建链基准（最多等待 12 帧）。
- 外部旋转识别阈值由 5° 收紧到 0.35°，慢速关键帧也能被识别；重锚同时清除旧旋转
  平滑状态，避免切动作后残留扭矩。
- 新增 `SOMA_POSE_REBASE` 结构化日志，记录每个 Chain 部位重建基准的耗时和稳定/超时原因。

## 1.0.1.0（2026-08-11）

- 工作室切换角色、加载动作或重播动作时，自动清除上一姿态的 Soma 形变，并在新姿态
  稳定两帧后再次复位求解器，不再需要手动点击 `Restore pose`。
- 角色、坐标及身体重载时先恢复旧骨骼，再释放原生链并按新骨架重建，避免旧骨骼引用和
  形变被新角色当作 Pristine Pose。
- 关闭运行指标时不再读取 Mono 堆或 Stopwatch，也不再为每个物理骨计算指标向量长度。
- Soma 面板关闭时卸载 Unity Input Harmony 补丁，并跳过鼠标查询，减少常驻输入开销。
- 版本、构建脚本、组合包和文档同步升级至 1.0.1.0；组合包配套稳定器升级至 1.2.1。

## 1.0.0.0（2026-08-09）

- 正式显示名更改为 `Soma Dynamics｜形体动力学控制器`。GUID、DLL、安装目录和角色卡
  数据键保持不变。
- 最终 UI 收口：顶部只保留一组全身控制和 `低 / 中 / 高`，删除部位页重复预设、
  `Apply now` 及重复说明；统一专业命名、中英文层级和按钮作用域。
- 低/中/高只调整五部位的三个目标值，不再改写求解模式；大腿、手臂、腹部可在预设后
  自由组合 Spring / Chain。模式选择改为明确动作按钮，修复互斥 Toggle 的选中锁死。
- 重做低/中/高三档：中档逐骨 Amp 精确采用作者常用 `MyPreset`，低档为 0.75 倍，
  高档统一为 1.30 倍；强度、柔软度、运动响应和两套求解器参数同步分级。
- 根据最终实机反馈，只有高档大腿 `Thigh02 Amp` 在 Spring 与 Chain 中固定为 `0.50`，
  中档仍保持 `MyPreset` 原值；胸部中/高档
  `摆动强度 Swing` 分别封顶 `0.50 / 0.60`，不改运行算法。
- 删除与各部位 `恢复推荐基线` 作用重复的顶部撤销按钮，减少状态快照和重复概念；
  配置文件仍保存完整五部位组合。
- 按用户测试流程移除插件内置自动载入场景、确定性动作驱动及对应配置，并删除已失效的
  沙盒启动脚本；保留静态参数测试、运行日志和离线分析工具。
- 所有效果量获得 2 倍增强空间：目标参数 0~2、Motion 原始量 0~10、Weight 0~2、
  Gravity ±0.4、逐骨 Amp 0~4、频率 0~5、轴向/旋转倍率 0~2；1~2 增强区仍受
  速度、位移、旋转与非有限状态保护。原生 DynamicBone 的归一化系数保持真实 0~1。
- 根据完整实机测试，移除效果不合格的胸部/臀部独立 Spring，包括运行组件、参数模型、
  卡片/XML 字段、UI、测试和文档；胸臀统一使用保留游戏碰撞的原生 DynamicBone 链。
- 实时参数应用不再调用 `ChaControl.ReSetupDynamicBoneBust`：反编译确认该包装方法会间接
  执行 `ResetPosition`。胸部改由 `setPtn(0, true)` 原位同步、臀部直接更新运行粒子，
  拖动全身/部位滑条时不再重置正在碰撞的胸部粒子。
- 卡片数据升至 v61、XML 升至 v4；v60 实验 Spring 键被安全忽略，旧原生链参数继续迁移。
- 结构化日志保留 `FPC_PRESET_APPLY` 与 `FPC_NATIVE_APPLY`，记录档位、保留模式和原生链
  实际匹配数。
- 将胸部、臀部原生 `DynamicBone_Ver02` 链纳入同一 UI、卡片数据与整套预设。
- 默认胸臀参数精确移植 BPC Auto Default 当前使用的 Bust/Hip `Soft.xml`。
- 胸臀只保留强度、柔软度、动作响应三个常用参数；高级页保留重力与逐骨基线说明。
- 完成替代后可停用 BreastPhysicsController、BPC Auto Default 与 DisableHipDynamicBones。
- 完整迁移 BPC 的裸身/胸罩/上衣及 7 个坐标状态，并提供胸部 3 级、臀部 2 级逐骨高级参数。
- 修复 1.0 首轮融合遗漏 Harmony 注册的问题：现在会实际拦截游戏
  `BustSoft.ReCalc` / `BustGravity.ReCalc` 对自定义参数的覆盖，并在日志中记录保护命中。
- 原生胸臀应用路径按 BPC 顺序重新对齐：整表同步胸部运行粒子，不再调用 BPC 原版
  不存在的 `SetWeight` / `ResetParticlesPosition`，避免碰撞持续期间重置粒子造成二次激振。
- 构建新增 BPC 兼容契约检查；保护补丁未注册或实时路径重新引入权重/粒子重置时拒绝打包。
- 修正 0.9 防抽搐连续低通造成的肉感衰减：保留最短角度修正，但仅在高帧率 MMD
  输入上用三采样中值剔除孤立尖峰，正常舞蹈频段不再被持续削弱，`MotionGain` / 舞蹈增强
  重新作用于完整动作输入。
- 修复右臀第二级骨 `cf_j_siri_R_01` 未匹配：左右骨名现在与 BPC 一样替换名称中任意
  `_R` 标记；运行日志分别报告 kind 2/3 的实际匹配数。
- 简单 UI 统一为目标模式：`摆动强度 / 柔顺度 / 动作响应` 在五个部位均为 0~2，且
  数值越高视觉效果越明显；胸臀 BPC Auto Soft 位于中点，旧卡和 v1 XML 自动保持原效果迁移。
- 重做 MMD 链式运动输入：骨骼旋转先换算为真实切向位移，与 Studio 方向轴平移使用同一
  世界距离模型；动作激发控制“粒子跟随量”的反方向，高值产生更大物理滞后，不再把粒子
  推着身体走而抵消甩动。
- 高档位采用 `MyPreset.xml` 的可见摆动方向重新收敛为非极端目标值，并在强度/动作目标
  较高时把安全可见范围从日常上限 1.35 倍渐进扩展到增强上限 2 倍；最短角度、中值尖峰保护、速度和写入
  限位全部保留。

## 0.9.0.0（2026-08-09）

- 修复链式大腿/小臂在 MMD Director 高帧率播放时偶发抽搐：锚点位移与角速度
  统一换算到60 FPS参考速度，再以按帧率等效的低通强度过滤实时计时尖峰。
- `Quaternion.ToAngleAxis` 输出统一映射到最短路径的 -180..180 度，避免微小反向
  转动偶尔变成359度并把切向驱动瞬间推到上限。
- 输入滤波状态在异常、父骨变化和外部重锚时同步清零，避免旧速度残留。

## 0.8.11.0（2026-08-09）

- 简单 UI 将“关闭 = 日常弹簧”的反向勾选改为两个明确模式按钮：`日常弹簧 Spring`
  与 `舞蹈链式 Chain`；整套预设按钮也直接标明目标求解器，减少隐藏状态和猜测。
- 修复默认预设语义：`稳定日常` 现在真正使用 Spring；`常用自然` 保持用户常用 Chain
  基线；`舞蹈增强` 使用柔软 Chain。Stable 实机门禁确认三部位为 Spring，27 项通过，
  零安全重置、零误重锚。
- 沙盒回归新增 `CardPath/CardLabel` 和 `run.json` 身份记录；新增跨卡体型矩阵门禁。
  Chain 已覆盖基线女性、第二张女性和 Bulky 男性（61 项），Spring 覆盖女性和 Bulky
  男性（42 项），各部位均有可测响应、峰值受控、零恢复事件，跨卡动态差异不超过 1.5 倍。
- 参数模型回归扩展至 574 项；正式构建保持零警告、零错误。

## 0.8.10.0（2026-08-09）

- Chain 与 Spring 的时间步、阻尼、回中和平滑改为按 60 FPS 参考强度指数合成；
  Chain 额外校正四骨大腿链的约束频率，30/60/约 157 FPS 三部位动态差异均通过
  25% 门禁，测试过程零安全重置、零误重锚。
- 修正 Spring 深层积分问题：不再截断 0.20 以上的阻尼；动态偏移改为围绕重力平衡点
  积分，避免硬档被伪振动推到位移上限；简单柔软度不再把旧 `JitterFreq` 推到大于 1
  的注能区间。
- Chain 与 Spring 均新增 0/0.25/0.5/0.75/1 五点柔软度实机门禁；大腿、手臂、
  小肚子动态 RMS 严格单调，峰值受控，零重置、零重锚。
- 新增统一 `FleshStateReset`，收敛父级变化、瞬移、外部重锚、禁用骨骼和异常恢复的
  重复状态清理；主编排文件由 1483 行降至约 1171 行。
- 性能日志排除指标字符串开销并增加内存来源、每帧堆增量和最大堆增量字段；当前约
  157 FPS 测试角色 Chain 三部位约 44.8 μs/帧、Spring 约 39.2 μs/帧。旧 Mono
  只能报告求解器区间 `mono_heap_delta=0`，这是“未观察到堆增长”，不是精确零分配证明。
- 参数模型回归扩展至 571 项，正式构建保持零警告、零错误。

## 0.8.9.0（2026-08-09）

- 重做弹簧柔软度曲线：紧/中/软端点改为有足够回中和阻尼的稳定组合，弹簧默认值
  对齐柔软度 0.5；旧卡迁移也不再恢复已过时的欠阻尼参数。
- 修复小肚子弹簧几乎没有动态的问题：写入死区随每骨 Amp 缩放，并补偿 0.25 倍
  小肚子 Amp 对输入驱动的重复衰减；输出仍受原有每骨位移上限保护。
- 新增 Spring 实机柔软度扫描并修正测试锚点为 `cf_j_waist01`；大腿、手臂、小肚子
  的动态 RMS 均随柔软度 0/0.5/1 单调增加，零安全重置、零误重锚。
- 所有卡片、XML、简单参数和文本输入统一经过有限数值边界；`NaN`、正负无穷与
  错误类型保留当前安全值，不再依赖无法拦截 NaN 的 `Mathf.Clamp`。
- 核心开始拆分：骨骼/链状态、SafetyGuard、运行指标、公共标量映射、Spring 策略与
  Chain 数值积分已独立；单/多粒子链共享速度积分和 leash 约束，删除重复实现。
- 新增无委托分配的逐部位 CPU 计时与 `performance.csv` 汇总；当前测试角色舞蹈场景
  Chain 三部位合计平均约 42.1 μs/帧，Spring 约 37.3 μs/帧，作为后续优化基线。
- 参数模型回归扩展至 560 项；Chain 与 Spring 两种模式均有沙盒动态门禁。

## 0.8.8.0（2026-08-09）

- 删除发布包中名不副实且未经验证的 `Soft / Realistic / Exaggerated` XML 默认预设；
  简单 UI 改为整套应用到大腿、手臂和小肚子的 `稳定 / 自然 / 舞蹈` 三档。
- `自然` 直接采用用户常用参数；`稳定` 收紧柔软度、强度与动作响应；`舞蹈` 使用
  柔软端点并适度增强响应。三档由代码中的版本化映射生成，不再维护重复底层 XML。
- 新增独立参数模型测试，覆盖 490 项默认值、往返映射、单调性、预设语义、输入限幅
  与旧死键；正式构建默认执行测试。
- 新增确定性动作驱动、五秒运行指标、柔软度扫描和沙盒回归门禁；链式柔软度 0/0.5/1
  已验证三个部位动态响应单调，`舞蹈` 档实机验证无重置、无重锚且峰值低于安全阈值。
- 修复小肚子单粒子链没有弹性回中、部分参数实际不起作用的问题；多粒子链的刚性和
  弹性修正限制在 0~1，避免参数组合导致单帧过冲。

## 0.8.7.0（2026-08-09）

- UI 默认进入简单调节，仅显示 `强度 / 柔软度 / 动作响应`；完整求解器参数、逐骨与
  轴向控制收进 `Advanced 高级`，旧卡与旧 XML 预设仍可读取。
- 新增部位感知的参数映射层：柔软度会成组调节阻尼、弹性、刚性、惯性、抖动频率
  与弹簧平滑，不再要求用户理解并逐项配平底层参数。
- 以本机最新 `NEW.xml` 实际调参为新角色默认与“我的常用基线”：大腿、手臂采用
  低阻尼/高惯性/低频链式配置，小肚子保留更克制的稳定配置。
- 删除从未进入现行物理计算的 8 个旧共享字段和两个未调用的 `Clone` 方法；旧卡中
  的额外键会被安全忽略，GUID 不变。
- 卡片与 XML 读取统一对全部物理参数做范围限制，坏值不能越过 UI 范围进入求解器；
  卡片数据版本升至 56。

## 0.8.6.4（2026-08-07）

- 修复舞蹈过程中小腹概率性消失：
  - 链式单粒子链（小腹）补上缺失的 NaN/Infinity 防护（多粒子链原有，单粒子漏了）；
  - 弹簧模式 NaN 防护现在会同时清掉旋转/加速度/角速度状态，并校验写入后的位置与旋转；
  - 写入前对旋转欧拉角做 ±30° 硬限幅，防止瞬时异常值污染骨骼旋转；
  - 检测到骨骼位置/旋转/缩放异常时自动恢复到初始姿态，不再把小腹“永久消失”。

## 0.8.6.3（2026-08-06）

- `Jitter freq` 下限从 0.2 放开到 0（弹簧/链式滑条、卡片与预设读取同步），
  需要更自由、更像胸部物理的甩动手感时，频率可调到 0~0.2。

## 0.8.6.2（2026-08-06）

- 小肚子链移除 `cf_s_spine03`（上段脊柱，刚性骨，不应抖动），只保留
  `cf_s_waist01`；链式模式新增单粒子链支持，小肚子在弹簧/链式下都能正常摆动。
- 小肚子每骨参数区现在只有 `waist01` 一行。

## 0.8.6.1（2026-08-06）

- 修复部件切换失效：Thigh/Arm/Belly 改用显式切换按钮，避免面板停在错误部位
  （症状：选小肚子却显示手臂/肩臂骨骼名）。
- 每骨参数区标题标注当前模式（Spring/Chain），便于确认正在编辑的部位。
- 说明：小肚子链固定为 `cf_s_waist01 + cf_s_spine03`；`spine03` 是上段脊柱，
  视觉上接近肩背，但属于小肚子链的一部分，不是肩臂骨（arm01 等）。

## 0.8.6（2026-08-06）

- 开放新参数并真实接入物理（面板实时可调，随卡片/预设保存）：
  - 弹簧模式新增：`Jitter freq`（抖动频率，0.2~2.5，缩放弹簧速度与弹性回中）、
    `Motion smooth`（响应平滑，0.05~0.5，控制加速度/角速度滤波，越小越柔）；
  - 链式模式新增：`Jitter freq`（抖动频率，0.2~2.5，缩放刚度/弹性修正）。
- 卡片数据版本升至 55（旧卡缺失字段自动使用新默认值；v54 为已归档坏版本）。

## 0.8.5.1（2026-08-06）

- 弹簧模式防积累（修复跳舞时大腿越跳越变形）：
  - 重锚检测从世界空间改为父空间局部判定，父骨快速旋转不再误触发重锚、
    不再把自身偏移固化进基准位置；
  - 弹簧 RC（RotCalc）改为基准旋转瞄准 + ±12° 限幅，消除“当前朝向反馈”
    导致的跳舞扭曲；
  - 内置后台慢速纠偏（`Auto fix spring drift`，默认开启）：检测到基准偏移
    持续 2 秒且无外部改写时，以约 0.03m/s 的速度拉回卡片原始姿态；
    正常抖动不受影响，可在配置中关闭。
- RC（RotCalc）默认全开：新建参数、内置预设（Soft/Realistic/Exaggerated）的
  每骨 RC 与共享 IsRotationCalc 默认都为 true，旧卡已存值不受影响。
- 修复“关闭部位/骨骼后腿部仍变形、Clear 无效”：
  - 部位开关关闭时物理立即恢复骨骼姿态；
  - 链式模式下禁用骨骼时位置+旋转完全复位；
  - 移除物理前先复位骨骼，避免重新启用后把变形姿态当“原始姿态”固化；
  - 链式 RC 不再旋转已禁用的骨骼。

## 0.8.5（2026-08-05）

- Dance response 上限从 3 放开到 5（UI、卡片读取、预设读取三处同步），默认仍 1。
- 弹簧减抖：默认 Weight 0.8→0.7、Damping 0.03→0.12、Inert 0.35→0.30；
  加速度平滑、X/Z 增益、驱动系数、弹簧速度阻尼下限一并调整。
- 链式增强：默认 Weight 0.7、Damping 0.30、Inert 0.40；舞蹈驱动系数上调、
  切向/速度限幅放宽，Dance response 2~5 时拖尾更明显。
- 多角色场景：角色列表按女性/男性分组显示、选中按实例记忆、点击行直接切换；
  参数按“名字+性别+性格”会话内记忆，同名同性别角色自动同步；
  `OnReload` 尊重 `maintainState`，场景刷新/换装不再强制重置参数。
- 兼容：移除 KKAPI 最低版本限制（`[BepInDependency("marco.kkapi")]`），
  低版本 KKAPI 环境也能加载。

## 0.8.1（2026-08-05）

- 插件显示名与游戏内面板标题统一为英文 `Flesh Physics Controller`
  （GUID 不变，旧卡兼容）；文件对话框、配置描述、日志前缀同步统一。
- Dance response 在弹簧/链式之间统一倍数：默认参数下 1=1x、2=2x、3=3x。
- README 重写：新增使用方法、弹簧/链式对比与分步教程。

## 0.8.0（2026-08-04）

- 插件更名“躯体肉感物理控制器”（GUID 不变，旧卡数据兼容）。
- 彻底移除 KneeF 膝盖骨，卡片数据版本升至 53。
- 新增手臂（Arm）与小肚子（Belly）部位，面板按部位独立调参。
- 修复跳舞时身体消失（小肚子链排除 `cf_s_waist02` 结构骨），加入 NaN 防护。
- 链式物理重做（消除“橡皮泥”）、ABMX 兼容、预设保存/加载等。
